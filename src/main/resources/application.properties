# Application name (you already have this)
spring.application.name=demo

# MySQL Database configuration (you already have this)
spring.datasource.url=jdbc:mysql://localhost:3306/financetracker
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Irony@aHunter123@
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update

# Keycloak Configuration
keycloak.auth-server-url=http://localhost:8080/auth
keycloak.realm=myrealm
keycloak.resource=myclient
keycloak.credentials.secret=myclientsecret
keycloak.public-client=true
keycloak.bearer-only=true
keycloak.use-resource-role-mappings=true

# Spring Security settings for Keycloak
keycloak.security-constraints[0].authRoles[0]=USER
keycloak.security-constraints[0].authRoles[1]=ADMIN
keycloak.security-constraints[0].urlPattern=/secure/*
